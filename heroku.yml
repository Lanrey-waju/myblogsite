setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: app/Dockerfile
release:
  image: web
  command:
  - python manage.py collectstatic --noinput
run:
  web: gunicorn app/my_blog.wsgi:application --bind 0.0.0.0:8000